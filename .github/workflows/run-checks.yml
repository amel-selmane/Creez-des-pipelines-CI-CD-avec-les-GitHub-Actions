name: Run Checks Workflow

on: push

jobs:
    run-checks:
        runs-on: ubuntu-latest

        steps:
            # - name: Checkout
            #   uses: actions/checkout@v4.1.1

            - name: install Node v18
              uses: npm install node@18